bluetooth

监听
bluetoothServerSocket = bluetoothAdapter.listenUsingRfcommWithServiceRecord(NAME_SECURE, MY_UUID_SECURE);
bluetoothServerSocket = bluetoothAdapter.listenUsingInsecureRfcommWithServiceRecord(NAME_INSECURE, MY_UUID_INSECURE);
while true bluetoothSocket = bluetoothServerSocket.accept();

连接
bluetoothSocket = bluetoothDevice.createRfcommSocketToServiceRecord(MY_UUID_SECURE);
bluetoothSocket = bluetoothDevice.createInsecureRfcommSocketToServiceRecord(MY_UUID_INSECURE);
bluetoothSocket.connect();

已连接
inputStream = bluetoothSocket.getInputStream();
outputStream = bluetoothSocket.getOutputStream();
byte[] buffer
接收：while true bytes = mmInStream.read(buffer);
发送：outputStream.write(buffer);

搜索：
if (bluetoothAdapter.isDiscovering()) {
    bluetoothAdapter.cancelDiscovery();
}
bluetoothAdapter.startDiscovery();
结果接收广播：
BluetoothDevice device = intent.getParcelableExtra(BluetoothDevice.EXTRA_DEVICE);

----------------------------------------------------------------------------------------

socket
